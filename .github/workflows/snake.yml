name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Generate snake animation
        uses: Platane/snk@v3
        with:
          github_user_name: GrigorevIvan
          outputs: |
            dist/github-snake.svg?color_snake=00ff88&color_dots=#ebedf0,#9be9a8,#40c463,#30a14e,#216e39&grid=1
            dist/github-snake-dark.svg?palette=github-dark&color_snake=00ff88&color_dots=#161b22,#0e4429,#006d32,#26a641,#39d353&grid=1
            
      - name: Deploy to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
          keep_files: false
